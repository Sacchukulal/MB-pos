[package]
name = "mb-core"
description = "Magic Bill billing rules: money, tax, discounts, charges, totals."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
# Test-only, never shipped: proves the id newtypes really do serialise as bare
# strings, which P04's columns and P08's generated TypeScript both rely on.
serde_json = "1"
