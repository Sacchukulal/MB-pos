{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the counter's own window is allowed to ask Tauri for. Deliberately short: this product's front end talks to Rust through our own commands and one event channel, and nothing else.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
